import React from "react";

export default function MovieCard() {

    const movieCardFunc = (array) => {
        const card = array.slice().map((aMovie) => {
            return (
                <div>
                    
                </div>
            )
        })
    }

  return card
  <div>
      
  </div>;
}
